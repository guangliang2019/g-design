import { App } from "vue";
import { GOptions } from "./type";
export declare const getComponentPrefix: (options?: GOptions | undefined) => string;
export declare const setGlobalConfig: (app: App, options?: GOptions | undefined) => void;
